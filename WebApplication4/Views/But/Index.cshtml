@model IEnumerable<WebApplication4.Models.ButModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.jobb.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.But)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.jobb.Titel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.But)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.butModelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.butModelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.butModelId })
        </td>
    </tr>
}

</table>
