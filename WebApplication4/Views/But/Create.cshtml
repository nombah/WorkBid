@model WebApplication4.Models.ButModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ButModel</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.But, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.But)
                @Html.ValidationMessageFor(model => model.But)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.jobbId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("jobbId", String.Empty)
                @Html.ValidationMessageFor(model => model.jobbId)
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
